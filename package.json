{
  "name": "fynda-market",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "db:test": "node scripts/db-test.mjs",
    "guardrails": "node scripts/guardrails.mjs",
    "verify": "npm run check && npm run build && npm run guardrails",
    "prebuild": "node scripts/emit-data.mjs",
    "deploy": "node scripts/deploy.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.4.0",
    "@types/pg": "^8.23.1",
    "pg": "^8.23.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.129.0"
  }
}
