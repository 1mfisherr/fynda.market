# AGENTS.md

Instructions for coding agents live in `CLAUDE.md` at the repository root; read that file. It is written for Claude Code and serves any agent that reads AGENTS.md. Directory-specific rules are in `.claude/rules/`.
