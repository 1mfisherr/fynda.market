---
/**
 * The date chips. Home offers four windows, a city page three — the same
 * control either way, so it is one component with a list rather than two
 * near-identical `.filters` rules in two pages.
 *
 * It renders buttons and nothing else. The page owns what filtering means for
 * its own list; the shared date arithmetic lives in lib/date-window.ts.
 */
import type { DateWindow } from '../lib/date-window';
import { DEFAULT_LOCALE, type Locale } from '../lib/i18n';
import { t } from '../lib/strings';

interface Props {
  windows?: DateWindow[];
  locale?: Locale;
}
const { windows = ['all', 'today', 'weekend', 'week'], locale = DEFAULT_LOCALE } = Astro.props;
const s = t(locale);
const LABELS: Record<DateWindow, string> = {
  all: s.filterAll,
  today: s.filterToday,
  weekend: s.filterWeekend,
  week: s.filterWeek,
};
---
<div class="filters gutter" role="group" aria-label={s.filterPeriodLabel}>
  {windows.map((window, i) => (
    <button class="button" type="button" aria-pressed={i === 0} data-filter={window}>{LABELS[window]}</button>
  ))}
</div>

<style>
  /* Wrap, never scroll. Four chips do not fit one phone line in any of our
     languages — "La semaine prochaine" is twice the width of "Alle" — and a
     sideways scrollbar under the hero read as a broken page. A second row of
     chips does not. */
  .filters { display: flex; flex-wrap: wrap; gap: var(--space-2); }
  .filters .button { padding-inline: var(--space-3); }
</style>
