---
/**
 * The radius view in en. One line, because the page itself is
 * components/RadiusView.astro — see the note there on why this is four static
 * files rather than a second dynamic route under [locale]/.
 */
import RadiusView from '../../components/RadiusView.astro';
---
<RadiusView locale="en" />
